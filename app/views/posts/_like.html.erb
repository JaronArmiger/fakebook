<%= form_with(model: current_user.likes.build, local: true) do |f| %>
  <%= hidden_field_tag :liked_post_id, post.id %>
  <%= hidden_field_tag :current_url, request.original_url %>
  <%= f.submit "like" %>
<% end %>