<%= form_with(model: current_user.likes.find_by(liked_post_id: post.id), local: true, method: 'delete') do |f| %>
  <%= hidden_field_tag :liked_post_id, post.id %>
  <%= hidden_field_tag :current_url, request.original_url %>
  <%= f.submit "unlike" %>
<% end %>