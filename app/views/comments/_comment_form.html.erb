<%= form_with(model: current_user.comments.build, remote: true) do |f| %>
  <%= hidden_field_tag :current_url, request.original_url %>
  <%= f.hidden_field :post_id, value: post.id %>
  <span>
    <%= f.text_field :content %>
    <%= f.submit "commenter" %>
  </span>
<% end %>