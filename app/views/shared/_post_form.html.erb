<%= form_with(model: @post, local: true) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <br>
  <%= f.label :content, "Vos pensees" %>
  <%= f.text_area :content %>
  <br>
  <%= f.submit "Partager" %>
<% end %>