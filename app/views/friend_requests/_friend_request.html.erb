<div class="friend_request">
	<% user = friend_request.from_user %>
	<%= link_to user.name, user_path(user) %>
	<%= link_to 'accepter', friendships_path(user_id: user.id), method: 'post' %>
	<%# link_to 'effacer', %>
	<%# render partial: 'friend_requests/accept_form', locals: { request_id: friend_request.id } %>
	<%= render partial: 'friend_requests/delete_form', locals: { request_id: friend_request.id } %>
	recu <%= friend_request.created_at.strftime("%b %d a %I:%M") %>
</div>
